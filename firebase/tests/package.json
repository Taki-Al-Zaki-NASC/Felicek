{
  "name": "felicek-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Firestore security rules tests for Felicek.",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --config ../firebase.json --project demo-felicek --only firestore \"node --test rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0",
    "firebase-tools": "^13.29.1"
  }
}
